chain:
  - name: sleep
    ref: core.local
    parameters:
      cmd: sleep "{{ sleep_sec }}"
      timeout: "{{ sleep_sec + 60 }}"
    on-success: do_respond
  - name: do_respond
    ref: st2.inquiry.respond
    parameters:
      id: "{{ inquiry_id }}"
      response:
        continue: true
